import { subpageMetadata, makeSubpage } from "../subpage";

export const dynamic = "force-dynamic";
export const generateMetadata = subpageMetadata("neighbourhood");
export default makeSubpage("neighbourhood");
